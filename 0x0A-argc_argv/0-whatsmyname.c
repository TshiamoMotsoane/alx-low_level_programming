#include "main.h"
#include <stdio.h>

/**
 * main - program that prints it's name
 * @argc: numbwe of arguments
 * @argv: array of string
 * Return: Always 0
 */

int main(int argc, char *argv[])
{

	(void) argc;

	printf("%s\n", argv[0]);
	return (0);
}
